export default {
  state: {
    account: 'test123',
  },
  reducers: {},
  effects: {
    async asyncLogout() {
      // const res = await Login.logOut();
      // if (res.code === 0) {
      //   window.location.href = '/login';
      // }
    },
  },
};
